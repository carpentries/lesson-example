image:
  name: carpentries/lesson-docker:latest
  entrypoint: [""]

variables:
  LC_ALL: "C.UTF-8"
  MAKEFLAGS: "-j 8"

pages:
  stage: deploy
  script:
      - make site
      - mv _site public
  artifacts:
      paths:
          - public
  only:
      - gh-pages
